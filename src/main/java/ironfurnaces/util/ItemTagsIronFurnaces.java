package ironfurnaces.util;

public class ItemTagsIronFurnaces {
    /**
    public static Fluid getOreDict(String oredict)
    {
        ResourceLocation tag = new ResourceLocation("forge", oredict);
        for (Fluid fluid : ForgeRegistries.FLUIDS.getValues())
        {
            if (fluid.defaultFluidState().getTags().toList().contains(tag))
            {
                return fluid;
            }
        }
        return null;
    }




     **/
}
